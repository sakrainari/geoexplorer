# GAS デプロイ手順（GeoExplorer 入力ツール）

入力ツールの「シートに追加」ボタンから、スプレッドシートの
**スポットマスター** と **解説シート** に同時に1行追加できるようにします。

> 対象スプレッドシート:
> https://docs.google.com/spreadsheets/d/1ystSEsdLxo6yj-YHZo_C4xKwc9usmFEHyuprtOX3sb4

---

## 事前準備：シート名の確認

スプレッドシートのタブ名が、以下と**完全一致**しているか確認してください（全角・スペースに注意）。

- `スポットマスター`
- `解説シート`

各シートの1行目はヘッダー（列名）にしておきます（2行目以降にデータが追加されます）。
違う名前にしている場合は、後述の `Code.gs` 冒頭の `MASTER_SHEET` / `DESC_SHEET` を書き換えてください。

---

## 手順

### 1. スクリプトエディタを開く
1. 対象のスプレッドシートを開く
2. メニュー **拡張機能 → Apps Script** をクリック

### 2. コードを貼り付ける
1. 既定の `コード.gs`（`Code.gs`）の中身をすべて削除
2. このリポジトリの [`gas/Code.gs`](gas/Code.gs) の内容を**まるごと貼り付け**
3. 保存（Ctrl+S / フロッピーアイコン）

### 3. ウェブアプリとしてデプロイ
1. 右上の **デプロイ → 新しいデプロイ** をクリック
2. 歯車アイコン（種類の選択）→ **ウェブアプリ** を選択
3. 設定:
   - **説明**: 任意（例：GeoExplorer 入力API）
   - **次のユーザーとして実行**: `自分`
   - **アクセスできるユーザー**: `全員`
     - ※「全員」にしないと、GitHub Pages から送信できません
4. **デプロイ** をクリック
5. 初回は権限の確認が出ます:
   - 「アクセスを承認」→ 自分のGoogleアカウントを選択
   - 「このアプリは確認されていません」と出たら、
     **詳細 → （プロジェクト名）に移動（安全ではないページ）** を選んで続行
   - スプレッドシートへのアクセスを **許可**
6. 表示される **ウェブアプリのURL**（`https://script.google.com/macros/s/＿＿＿＿/exec`）をコピー

### 4. 動作確認（任意）
コピーしたURLをそのままブラウザのアドレスバーに貼って開くと、
JSONで以下のような状態が表示されれば接続成功です。

```json
{ "ok": true, "spreadsheet": "...", "masterSheet": "スポットマスター（最終行:1）", "nextId": "SPOT-0001" }
```

### 5. 入力ツールにURLを設定
1. [`index.html`](index.html) を開く
2. 上の方にある次の行を探す:
   ```js
   const GAS_ENDPOINT = "";
   ```
3. `""` の中にコピーしたURLを貼り付ける:
   ```js
   const GAS_ENDPOINT = "https://script.google.com/macros/s/XXXXXXXX/exec";
   ```
4. 保存して push（GitHub Pages に反映）

これで公開URL https://sakrainari.github.io/geoexplorer/ の
「＋ シートに追加」ボタンから書き込みできます。

---

## 仕様メモ

- **ID自動採番**: スポットマスターA列の既存IDの末尾数字の最大値 + 1。
  `SPOT-0001`, `SPOT-0002` … の形式（接頭辞・桁数は `Code.gs` の `ID_PREFIX` / `ID_PAD` で変更可）。
- スポットマスターと解説シートには**同じID**が書き込まれます。
- タグ・世界遺産登録基準は「、」区切りの文字列でセルに入ります。
- 同時実行による採番衝突を防ぐため `LockService` を使用しています。

## コードを変更したら

`Code.gs` を編集した後は、**デプロイ → デプロイを管理 → 鉛筆（編集）→ バージョン:新バージョン → デプロイ**
で再デプロイしてください（URLは変わりません）。
新しいデプロイを作るとURLが変わるので、既存デプロイの「編集」での更新が便利です。
